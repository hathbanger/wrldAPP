<p id="notice"><%= notice %></p>
<strong><%= @place.name %></strong><br>

  <div class="f32">
    <div class="flag selected" data-cc=<%= @place.iso2 %> data-country_name=<%= @place.name %> data-locale=<%= @place.iso2 %> title=<%= @place.name %>>&nbsp;</div>
</div>
<p>
  	Capital City: <%= @place.capCity %><br>
  	Population: <%= @place.population %><br>
	Temperature: <%= @place.temp %><br>
	Humidity: <%= @place.humidity %><br>
	Dewpoint: <%= @place.dew %><br>
	Wind Speed: <%= @place.windSpeed %><br>
	Wind Direction: <%= @place.windDir %><br>
	Forcast and Conditions: <%= @place.forcastAndConditions %><br>
	Current Weather: <%= @place.currWeather %><br>
  	<%= @place.longitude %><br>
  	<%= @place.latitude %><br>
  	<%= @place.rainData %><br>
  	<%= @place.tempData %><br>
</p>

<div id="chart"></div>
<%= link_to 'Edit', edit_place_path(@place) %> |
<%= link_to 'Back', places_path %>

<script type="text/javascript">
$(document).ready(function(){	


	console.log(gon.iso2);

	$('.f32 li').addClass(gon.iso2)

var chart = c3.generate({
    data: {
        // iris data from R
        columns: [
            ['data1', 30],
            ['data2', 120],
        ],
        type : 'pie',
        onclick: function (d, i) { console.log("onclick", d, i); },
        onmouseover: function (d, i) { console.log("onmouseover", d, i); },
        onmouseout: function (d, i) { console.log("onmouseout", d, i); }
    }
});	
})

</script>
